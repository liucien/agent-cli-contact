{
  "name": "@workbench/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:sidecar": "bun build --compile --minify ../gateway/src/index.ts --outfile src-tauri/binaries/gateway-aarch64-apple-darwin",
    "build": "pnpm build:sidecar && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "bun": "^1.4.2"
  }
}
