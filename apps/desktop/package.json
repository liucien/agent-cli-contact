{
    "name": "@agent-cli-contact/desktop",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build:sidecar": "bun build --compile --minify ../gateway/src/index.ts --outfile src-tauri/binaries/gateway-aarch64-apple-darwin",
        "build:dmg": "./scripts/make-dmg.sh",
        "build": "pnpm build:sidecar && tauri build && pnpm build:dmg",
        "tauri": "tauri"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.11.4",
        "bun": "^1.4.2"
    }
}
